use criterion::{criterion_group, criterion_main, Criterion};

fn terminal_benchmarks(_c: &mut Criterion) {
    // Placeholder: terminal benchmarks will be added in plan 07-04
}

criterion_group!(benches, terminal_benchmarks);
criterion_main!(benches);
