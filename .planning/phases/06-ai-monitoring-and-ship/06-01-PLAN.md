---
phase: 06-ai-monitoring-and-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/monitor/mod.rs
  - src/monitor/intervention.rs
  - src/monitor/patterns.rs
  - src/toast/mod.rs
  - src/toast/renderer.rs
  - src/theme/mod.rs
  - src/grid/panel.rs
  - src/terminal/state.rs
  - src/app.rs
  - src/settings.rs
autonomous: true
requirements: [AI-01, AI-03]
must_haves:
  truths:
    - "Each panel header displays a colored resource health dot (green/yellow/red)"
    - "Resource dot color updates every 2 seconds based on process CPU usage"
    - "Hovering the resource dot shows a tooltip with CPU % and RAM MB"
    - "Toasts appear in bottom-right stack (max 3 visible) and auto-dismiss"
  artifacts:
    - path: "src/monitor/mod.rs"
      provides: "ResourceMonitor background poller, ResourceState, ResourceUpdate"
      exports: ["ResourceMonitor", "ResourceState", "ResourceUpdate"]
    - path: "src/toast/mod.rs"
      provides: "ToastManager, Toast, ToastType"
      exports: ["ToastManager", "Toast", "ToastType"]
    - path: "src/toast/renderer.rs"
      provides: "build_toast_quads, build_toast_labels"
      exports: ["build_toast_quads", "build_toast_labels"]
    - path: "src/monitor/patterns.rs"
      provides: "PatternConfig, InterventionPattern, load from ~/.myco/patterns.json"
      exports: ["PatternConfig", "InterventionPattern"]
    - path: "src/monitor/intervention.rs"
      provides: "InterventionDetector with pattern matching"
      exports: ["InterventionDetector"]
  key_links:
    - from: "src/monitor/mod.rs"
      to: "src/app.rs"
      via: "UserEvent::ResourceUpdate channel"
      pattern: "UserEvent::ResourceUpdate"
    - from: "src/app.rs"
      to: "src/toast/renderer.rs"
      via: "build_toast_quads called in build_quads"
      pattern: "toast::renderer::build_toast_quads"
---

<objective>
Resource monitoring with visible health indicators and unified toast system.

Purpose: Deliver end-to-end resource awareness — user launches app, opens a terminal, and immediately sees a colored dot in the panel header reflecting the process's CPU usage. Hovering shows exact stats. The toast system is extracted from settings and generalized for use by both settings conflict toasts and future intervention alerts.

Output: Working resource dot in panel headers, tooltip on hover, unified toast system rendering, background polling via sysinfo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-monitoring-and-ship/06-CONTEXT.md
@.planning/phases/06-ai-monitoring-and-ship/06-RESEARCH.md
@.planning/phases/06-ai-monitoring-and-ship/06-PATTERNS.md
@.planning/phases/06-ai-monitoring-and-ship/06-UI-SPEC.md

<interfaces>
From src/renderer/quad_renderer.rs:
```rust
#[repr(C)]
#[derive(Copy, Clone, Debug, bytemuck::Pod, bytemuck::Zeroable)]
pub struct QuadInstance {
    pub position: [f32; 2],
    pub size: [f32; 2],
    pub color: [f32; 4],
    pub corner_radius: f32,
    pub _padding: f32,
}
```

From src/renderer/text_renderer.rs:
```rust
pub struct TextLabel {
    pub text: String,
    pub x: f32,
    pub y: f32,
    pub width: f32,
    pub height: f32,
    pub font_size: f32,
    pub color: GlyphonColor,
}
```

From src/app.rs:
```rust
pub enum UserEvent {
    TerminalEvent,
    FileChanged(Vec<std::path::PathBuf>),
    CanvasMessage(PanelId, String),
    #[cfg(target_os = "macos")]
    MenuAction(u32),
}
```

From src/grid/panel.rs:
```rust
pub struct Panel {
    pub id: PanelId,
    pub panel_type: PanelType,
    pub title: String,
    pub file_path: Option<PathBuf>,
    pub canvas_id: Option<String>,
}
```

From src/theme/mod.rs (relevant fields):
```rust
pub struct Theme {
    pub background: [f32; 4],
    pub panel_background: [f32; 4],
    pub divider_hover: [f32; 4],
    pub success: [f32; 4],
    pub warning: [f32; 4],
    pub bg_secondary: [f32; 4],
    pub fg_secondary: [f32; 4],
    pub fg_primary: [f32; 4],
    pub border: [f32; 4],
    pub unfocused_overlay: [f32; 4],
}
```

From src/terminal/state.rs:
```rust
pub struct TerminalState {
    pub term: Arc<FairMutex<Term<MycoEventListener>>>,
    pub event_loop_sender: EventLoopSender,
    pub exited: bool,
    // ... (no child_pid field yet)
}
```

From src/settings.rs:
```rust
pub struct NotificationToast {
    pub message: String,
    pub undo_action_id: String,
    pub undo_keys: Vec<KeyCombo>,
    pub shown_at: Instant,
}
const TOAST_DURATION: Duration = Duration::from_secs(3);
const TOAST_WIDTH: f32 = 280.0;
```
</interfaces>
</context>

## Phase Goal

**As a** workspace user, **I want to** see resource health indicators on each panel and receive toast notifications when processes need attention, **so that** I can monitor AI agents running in my workspace without constantly switching panels.

<tasks>

<task type="auto">
  <name>Task 1: Toast system extraction, resource monitor module, and theme extensions</name>
  <files>
    Cargo.toml,
    src/main.rs,
    src/monitor/mod.rs,
    src/monitor/intervention.rs,
    src/monitor/patterns.rs,
    src/toast/mod.rs,
    src/toast/renderer.rs,
    src/theme/mod.rs,
    src/grid/panel.rs,
    src/terminal/state.rs
  </files>
  <read_first>
    src/settings.rs (lines 44-47 TOAST constants, 196-228 NotificationToast struct, 445-466 toast creation/expiration, 912-946 build_toast_quads, 1454-1492 build_toast_labels),
    src/watcher/mod.rs (background task + EventLoopProxy pattern),
    src/config/global.rs (load_global_preferences with file-size check fallback pattern),
    src/theme/mod.rs (Theme struct, from_definition method),
    src/theme/definition.rs (ThemeBase with error field),
    src/grid/panel.rs (Panel struct),
    src/terminal/state.rs (TerminalState struct and new() method, lines 48-198),
    src/main.rs (module declarations),
    Cargo.toml (dependencies section)
  </read_first>
  <action>
    **1. Add dependencies to Cargo.toml:**
    - Add `sysinfo = "0.39"` to [dependencies]
    - Add `libc = "0.2"` to [dependencies]

    **2. Add module declarations to src/main.rs:**
    - Add `mod monitor;` and `mod toast;` after the existing `mod watcher;` line

    **3. Create src/toast/mod.rs — unified toast system (per D-14):**
    - Define `ToastType` enum: `Conflict`, `Intervention`, `Info`, `Error`
    - Define `Toast` struct with fields: `id: u64`, `toast_type: ToastType`, `message: String`, `attribution: Option<String>`, `source_panel: Option<PanelId>`, `action_text: Option<String>`, `shown_at: Instant`, `duration: Duration`
    - Define `ToastManager` struct with: `toasts: Vec<Toast>`, `next_id: u64`, `suppressed: HashMap<String, HashSet<PanelId>>`
    - `ToastManager::new()` — empty state
    - `ToastManager::add(toast_type, message, attribution, source_panel, action_text, duration) -> u64` — creates toast, returns ID, enforces max 3 visible (per security threat: unbounded toast spam)
    - `ToastManager::dismiss(toast_id)` — remove specific toast
    - `ToastManager::tick()` — remove expired toasts (retain where `shown_at.elapsed() < duration`)
    - `ToastManager::suppress_pattern(pattern_id, panel_id)` — add to suppressed map (per D-07)
    - `ToastManager::is_suppressed(pattern_id, panel_id) -> bool`
    - Rate limiting: max 1 toast per pattern per panel per 10 seconds (check in `add()`)
    - Constants: `INTERVENTION_TOAST_DURATION = Duration::from_secs(8)` (D-13), `INFO_TOAST_DURATION = Duration::from_secs(3)`, `MAX_VISIBLE_TOASTS: usize = 3`
    - Include `#[cfg(test)] mod tests` with:
      - `test_toast_lifecycle`: create toast, verify it exists, advance time past duration, tick, verify removed
      - `test_max_toasts`: add 4 toasts, verify only 3 visible
      - `test_suppression`: suppress pattern, verify `is_suppressed` returns true
      - `test_rate_limiting`: add same pattern/panel twice within 10s, verify second rejected

    **4. Create src/toast/renderer.rs:**
    - `pub fn build_toast_quads(toast_manager: &ToastManager, viewport_width: f32, viewport_height: f32, theme: &Theme, quads: &mut Vec<QuadInstance>)`
      - Position: `toast_x = viewport_width - 280.0 - 16.0`, `toast_base_y = viewport_height - 16.0`
      - For each toast (take MAX_VISIBLE_TOASTS): background quad (bg_secondary, corner_radius 4.0, size [280, 48]), left accent bar (2px wide, full height, color from toast_type: Intervention=theme.warning, Info/Conflict=theme.divider_hover, Error=theme.error)
      - Stack direction: upward (each toast y = toast_base_y - (i+1) * 56)
    - `pub fn build_toast_labels(toast_manager: &ToastManager, viewport_width: f32, viewport_height: f32, theme: &Theme, labels: &mut Vec<TextLabel>)`
      - Message at 13px font, x=toast_x+12, y=toast_y+8, color=fg_primary
      - Attribution at 11px font, x=toast_x+12, y=toast_y+28, color=fg_secondary
      - Action link (if present) at 13px, x=toast_x+192, y=toast_y+16, color=divider_hover

    **5. Create src/monitor/mod.rs — ResourceMonitor and ResourceState:**
    - `pub mod intervention;` and `pub mod patterns;`
    - `ResourceState` struct: `cpu_percent: f32`, `memory_bytes: u64`, `last_updated: Instant`
    - `ResourceUpdate` struct: `pid: u32`, `cpu_percent: f32`, `memory_bytes: u64`
    - `ResourceMonitor` struct owns a `std::thread::JoinHandle<()>` (background thread)
    - `ResourceMonitor::new(proxy: EventLoopProxy<UserEvent>) -> Self`:
      - Spawns a background thread that:
        1. Creates `sysinfo::System::new()` (no `_all`)
        2. Loops every 2 seconds (per D-03): receives PID list from a `std::sync::mpsc::Receiver<Vec<u32>>`, calls `system.refresh_processes_specifics(ProcessesToUpdate::Some(&pids), true, ProcessRefreshKind::nothing().with_cpu().with_memory())`, collects results, sends `proxy.send_event(UserEvent::ResourceUpdate(updates))`
      - Initial "priming" refresh on first loop iteration (sysinfo returns 0% on first call)
    - `ResourceMonitor::update_tracked_pids(&self, pids: Vec<u32>)` — sends new PID list to background thread via the sender
    - Add `UserEvent::ResourceUpdate(Vec<ResourceUpdate>)` variant to UserEvent enum in src/app.rs
    - `dot_color(cpu_percent: f32, theme: &Theme) -> [f32; 4]` helper:
      - `< 50.0` => `theme.success` (green, per D-01)
      - `50.0..=100.0` => `theme.warning` (yellow)
      - `> 100.0` => `theme.error` (red)
    - Include `#[cfg(test)] mod tests` with:
      - `test_dot_color_thresholds`: verify green at 10%, yellow at 75%, red at 150%
      - `test_resource_state_default`: ResourceState starts at 0/0

    **6. Create src/monitor/patterns.rs — pattern config loading (per D-06):**
    - `PatternConfig` struct with `patterns: Vec<InterventionPattern>`
    - `InterventionPattern` struct: `id: String`, `tool_name: String`, `matchers: Vec<String>`, `message_template: Option<String>`
    - `const MAX_PATTERNS_FILE_SIZE: u64 = 1_048_576` (1MB, per threat model path traversal/DoS)
    - `PatternConfig::builtin()` — returns built-in patterns:
      - `claude_code_permission`: matchers `["Do you want to proceed?", "(y/n)", "Allow?"]` (simple substring per research recommendation Q2)
      - `sudo_prompt`: matchers `["Password:", "[sudo] password for"]`
    - `PatternConfig::load() -> Self`:
      - Path: `dirs::home_dir().map(|h| h.join(".myco").join("patterns.json"))` (fixed path only, per threat model)
      - Check file size < MAX_PATTERNS_FILE_SIZE before reading
      - Parse with serde_json; on any error, warn and return `builtin()`
      - Limit total number of patterns to 100 (DoS protection)
      - Limit individual matcher string length to 200 chars (ReDoS protection per threat model)
    - Include `#[cfg(test)] mod tests` with:
      - `test_builtin_patterns`: verify Claude Code and sudo patterns exist
      - `test_load_nonexistent`: verify returns builtin when path doesn't exist
      - `test_serialization_roundtrip`: serialize builtin, deserialize, compare

    **7. Create src/monitor/intervention.rs — intervention detector stub (per D-05):**
    - `InterventionDetector` struct: `patterns: PatternConfig`, `last_scan: HashMap<PanelId, Instant>`
    - `InterventionDetector::new() -> Self`: loads `PatternConfig::load()`
    - `InterventionDetector::scan_text(text: &str) -> Vec<(String, String)>`:
      - For each pattern, for each matcher, check `text.contains(matcher)` (substring matching, not regex, per research recommendation)
      - Returns vec of `(pattern_id, tool_name)` for all matches
    - `InterventionDetector::should_scan(panel_id: PanelId) -> bool`:
      - Rate limit: at most once per 2 seconds per panel
    - Include `#[cfg(test)] mod tests` with:
      - `test_pattern_match_claude`: text containing "Do you want to proceed?" matches claude_code_permission
      - `test_pattern_match_sudo`: text containing "[sudo] password for" matches sudo_prompt
      - `test_no_false_positive`: normal terminal text has no matches

    **8. Extend src/theme/mod.rs — add error field to Theme:**
    - Add `pub error: [f32; 4]` field to Theme struct (after `warning`)
    - In `from_definition()`: add `let error = hex_to_linear(&def.base.error);` and assign `error` in the struct literal
    - In the overrides match block: add `"error" => theme.error = color,`
    - The ThemeBase already has an `error` field (verified in definition.rs line 27)

    **9. Extend src/grid/panel.rs — add frozen field:**
    - Add `pub frozen: bool` field to Panel struct
    - Add `pub child_pid: Option<u32>` field to Panel struct
    - Initialize both as `false` / `None` in all `Panel::new_*` constructors

    **10. Extend src/terminal/state.rs — capture child PID (per D-03, research Pattern 2):**
    - Add `pub child_pid: Option<u32>` field to TerminalState struct
    - In `TerminalState::new()`, AFTER `let pty = tty::new(&pty_config, window_size, 0)?;` (line 157) and BEFORE `let event_loop = EventLoop::new(...)` (line 161), insert: `let child_pid = pty.child().id();`
    - Store `child_pid: Some(child_pid)` in the Ok(Self { ... }) return struct
    - Only signal PIDs we ourselves spawned (security: T-06-02 constraint satisfied by capturing at creation time)
  </action>
  <verify>
    <automated>cargo test --lib toast:: monitor:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `sysinfo = "0.39"` and `libc = "0.2"`
    - src/main.rs contains `mod monitor;` and `mod toast;`
    - src/toast/mod.rs contains `pub struct ToastManager` and `pub struct Toast` and `pub enum ToastType`
    - src/toast/mod.rs contains `const MAX_VISIBLE_TOASTS: usize = 3`
    - src/toast/renderer.rs contains `pub fn build_toast_quads` and `pub fn build_toast_labels`
    - src/monitor/mod.rs contains `pub struct ResourceMonitor` and `pub struct ResourceState` and `pub fn dot_color`
    - src/monitor/mod.rs contains `UserEvent::ResourceUpdate`
    - src/monitor/patterns.rs contains `pub struct PatternConfig` and `pub fn load`
    - src/monitor/patterns.rs contains `const MAX_PATTERNS_FILE_SIZE: u64 = 1_048_576`
    - src/monitor/intervention.rs contains `pub struct InterventionDetector` and `pub fn scan_text`
    - src/theme/mod.rs contains `pub error: [f32; 4]`
    - src/grid/panel.rs contains `pub frozen: bool` and `pub child_pid: Option<u32>`
    - src/terminal/state.rs contains `pub child_pid: Option<u32>` and `pty.child().id()`
    - `cargo test --lib` passes with no failures
  </acceptance_criteria>
  <done>
    All new modules compile, tests pass for toast lifecycle/suppression/rate-limiting, pattern matching detects Claude Code prompts, dot_color returns correct thresholds, Theme has error field, Panel has frozen+child_pid fields, TerminalState captures child PID at creation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire resource dot into panel headers and toast rendering into app render loop</name>
  <files>
    src/app.rs,
    src/settings.rs
  </files>
  <read_first>
    src/app.rs (lines 20-27 UserEvent enum, 75 PANEL_TITLE_HEIGHT, 304-420 process_action, 1770-1810 panel header section of build_quads, 1858-1904 panel quad rendering loop),
    src/settings.rs (lines 196-228 NotificationToast struct, 445-466 toast push/tick, 647 build_toast_quads call, 912-946 build_toast_quads impl, 1075 build_toast_labels call, 1454-1492 build_toast_labels impl, 1703-1705 toast click detection),
    src/toast/mod.rs (from Task 1),
    src/toast/renderer.rs (from Task 1),
    src/monitor/mod.rs (from Task 1 — ResourceMonitor, ResourceUpdate, dot_color)
  </read_first>
  <action>
    **1. Add UserEvent::ResourceUpdate variant to src/app.rs:**
    - Add `ResourceUpdate(Vec<crate::monitor::ResourceUpdate>)` to the `UserEvent` enum

    **2. Add App state fields for resource monitoring and toasts:**
    - Add to the App struct: `resource_monitor: Option<monitor::ResourceMonitor>`, `resource_states: HashMap<u32, monitor::ResourceState>`, `toast_manager: toast::ToastManager`, `tooltip_state: Option<TooltipState>`
    - Define `TooltipState` struct locally in app.rs: `panel_id: PanelId`, `cpu_percent: f32`, `memory_bytes: u64`, `x: f32`, `y: f32`, `hover_start: Instant`
    - In App::new() or initialization: create `ResourceMonitor::new(proxy.clone())`, create empty `ToastManager::new()`, `resource_states: HashMap::new()`, `tooltip_state: None`

    **3. Handle UserEvent::ResourceUpdate in the event loop:**
    - In the UserEvent match block: when `ResourceUpdate(updates)` received, for each update store/update `resource_states` (keyed by pid), setting `last_updated: Instant::now()`
    - After updating, request window redraw

    **4. Wire resource dot into panel header rendering (build_quads):**
    - In the panel header rendering loop (near line 1858 where close button quad is created):
      - For each panel: look up child_pid from the Panel struct (or from terminal_manager for terminal panels)
      - If child_pid exists and resource_states has an entry: compute color via `monitor::dot_color(cpu_percent, &self.theme)`
      - If no process: use `self.theme.fg_secondary`
      - Render an 8x8 QuadInstance with corner_radius=4.0 at position: `x = close_x - 24.0`, `y = py_offset + 10.0` (vertically centered in 28px header)
    - Sync child_pid from TerminalState to Panel: when terminal is created, store `panel.child_pid = terminal_state.child_pid`

    **5. Render tooltip on hover (build_quads and build_labels):**
    - Track mouse position relative to resource dots
    - When mouse is over a resource dot for 300ms (check in mouse move handler), set `tooltip_state = Some(TooltipState { ... })`
    - In build_quads: if tooltip_state is Some, render a 160x52 quad at tooltip position (bg_secondary, corner_radius 4.0, 1px border)
    - In build_labels: render "CPU: {n}%" at 13px and "RAM: {n} MB" at 13px inside tooltip bounds
    - Clear tooltip_state when mouse moves away from both dot and tooltip area

    **6. Wire unified toast rendering into build_quads/build_labels:**
    - In build_quads (after all panel content, before settings overlay): call `toast::renderer::build_toast_quads(&self.toast_manager, width, height, &self.theme, &mut quads)`
    - In the label rendering section: call `toast::renderer::build_toast_labels(&self.toast_manager, width, height, &self.theme, &mut labels)`
    - Call `self.toast_manager.tick()` each frame (or on timer) to expire old toasts

    **7. Migrate settings toast to use ToastManager (per D-14):**
    - In src/settings.rs: change `NotificationToast` creation to instead call the shared `toast_manager.add(ToastType::Conflict, message, None, None, Some("Undo".to_string()), Duration::from_secs(3))`
    - The settings module needs a mutable reference to the toast_manager (pass as parameter or use the App-level manager)
    - Remove the settings-local toast rendering code (build_toast_quads and build_toast_labels from settings.rs) since the shared system now handles it
    - Keep the undo click logic: when toast clicked with action "Undo", dispatch the undo action. Wire this through a new `InputAction::ToastAction { toast_id: u64 }` or handle in the toast click detection area

    **8. Update PID tracking on terminal creation:**
    - When a terminal panel is created (in process_action or wherever terminals spawn): after creating TerminalState, store the child_pid in the Panel struct AND call `resource_monitor.update_tracked_pids(all_active_pids)`
    - When a terminal panel is closed: remove its PID from tracking, call `resource_monitor.update_tracked_pids(remaining_pids)`

    **9. Toast click handling:**
    - In mouse click detection: check if click is within any visible toast bounds
    - If toast has `source_panel`: dispatch `InputAction::FocusPanel { panel_id }` on click
    - Call `toast_manager.dismiss(toast_id)` after handling click
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app.rs contains `UserEvent::ResourceUpdate`
    - src/app.rs contains `resource_monitor` field and `toast_manager` field
    - src/app.rs contains `dot_color` call within the panel header rendering loop
    - src/app.rs contains `toast::renderer::build_toast_quads` call
    - src/app.rs contains `toast::renderer::build_toast_labels` call
    - src/app.rs contains `toast_manager.tick()` call
    - src/settings.rs no longer contains `fn build_toast_quads` (moved to toast module)
    - `cargo build` succeeds with no errors
    - `cargo test --lib` passes
  </acceptance_criteria>
  <done>
    Resource dot renders in each panel header with green/yellow/red color from live polling. Tooltip shows on hover after 300ms. Unified toast system renders in bottom-right corner. Settings conflict toasts now use the shared ToastManager. Application compiles and all tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Resource health dots in panel headers with live 2-second polling via sysinfo, hover tooltip showing CPU% and RAM, and unified toast system replacing the settings-local toast rendering.</what-built>
  <how-to-verify>
    1. Run `cargo run` to launch the application
    2. Open a terminal panel (should see a dot in the panel header, likely green/gray)
    3. In the terminal, run `yes > /dev/null` to spike CPU — dot should turn yellow/red within 4 seconds
    4. Hover over the resource dot — tooltip should appear after ~300ms showing "CPU: N%" and "RAM: N MB"
    5. Stop hovering — tooltip should disappear
    6. Kill the `yes` command (Ctrl+C) — dot should return to green within 4 seconds
    7. Open Settings (Cmd+,), rebind a shortcut to trigger a conflict — verify the toast still appears in bottom-right with "Undo" link
    8. Verify canvas/markdown panels show a gray/secondary dot (no process to monitor)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with dot rendering, tooltip, or toast behavior</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.myco/patterns.json -> app | Untrusted JSON file read from filesystem |
| Background thread -> main thread | Resource data crosses thread boundary via channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | patterns.rs | mitigate | Limit pattern file to 1MB, cap 100 patterns, cap matcher string to 200 chars. Use substring matching only (no regex engine). |
| T-06-03 | Denial of Service | toast/mod.rs | mitigate | Max 3 visible toasts enforced in ToastManager::add(). Rate limit 1 per pattern per panel per 10s. |
| T-06-04 | Information Disclosure | patterns.rs | mitigate | Fixed path `~/.myco/patterns.json` only. Never construct path from user/process input. |
</threat_model>

<verification>
- `cargo test --lib` passes (toast lifecycle, pattern matching, dot color thresholds)
- `cargo build` succeeds
- Visual: resource dots visible in panel headers
- Visual: tooltip appears on hover with stats
- Visual: toasts render in bottom-right stack
</verification>

<success_criteria>
- Resource dot visible in every panel header, colored by CPU threshold
- Tooltip shows CPU% and RAM on hover after 300ms delay
- Toast system renders max 3 toasts in bottom-right, auto-dismisses after configured duration
- Settings conflict toast still works via shared ToastManager
- All unit tests green for toast, patterns, dot_color
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-monitoring-and-ship/06-01-SUMMARY.md`
</output>
