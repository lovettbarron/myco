---
phase: 01-window-grid-and-build-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/renderer/quad_renderer.rs
  - src/renderer/text_renderer.rs
  - src/renderer/mod.rs
  - src/grid/mod.rs
  - src/grid/layout.rs
  - src/grid/panel.rs
  - src/platform/mod.rs
  - src/platform/macos.rs
  - src/app.rs
autonomous: true
requirements:
  - GRID-01
user_setup: []

must_haves:
  truths:
    - "User launches the app and sees a single themed panel filling the window body"
    - "Panel displays a centered 'Placeholder' type label and the title bar shows 'Myco > Untitled Project' breadcrumb"
    - "Window can be resized and the panel reflows to fill the new size"
  artifacts:
    - path: "src/renderer/quad_renderer.rs"
      provides: "Instanced colored rectangle rendering"
      exports: ["QuadRenderer", "QuadInstance"]
    - path: "src/renderer/text_renderer.rs"
      provides: "glyphon text rendering wrapper"
      exports: ["TextEngine"]
    - path: "src/grid/layout.rs"
      provides: "taffy CSS Grid wrapper"
      exports: ["GridLayout"]
    - path: "src/grid/panel.rs"
      provides: "Panel data model"
      exports: ["Panel", "PanelId", "PanelType"]
    - path: "src/platform/macos.rs"
      provides: "Custom title bar and traffic light setup"
      exports: ["setup_custom_title_bar"]
  key_links:
    - from: "src/app.rs"
      to: "src/grid/layout.rs"
      via: "App owns GridLayout, calls compute() on resize"
      pattern: "grid.*compute"
    - from: "src/renderer/mod.rs"
      to: "src/renderer/quad_renderer.rs"
      via: "Renderer calls quad_renderer.render() then text_renderer.render() in same pass"
      pattern: "quad_renderer.*render"
    - from: "src/renderer/mod.rs"
      to: "src/renderer/text_renderer.rs"
      via: "Renderer calls text_engine.prepare() then text_engine.render() after quads"
      pattern: "text_engine.*render"
    - from: "src/grid/layout.rs"
      to: "src/renderer/quad_renderer.rs"
      via: "Panel rects from taffy layout become QuadInstance data"
      pattern: "QuadInstance"
    - from: "src/app.rs"
      to: "src/platform/macos.rs"
      via: "App calls setup_custom_title_bar after window creation in resumed()"
      pattern: "setup_custom_title_bar"
---

<objective>
Renderers, grid layout, and platform integration -- completing the visible single-panel display.

Purpose: Build the quad renderer, text renderer, CSS Grid layout engine, panel model, and macOS platform layer on top of Plan 01-01's scaffold. After this plan, the user sees a themed dark panel with text labels ("Myco > Untitled Project" breadcrumb, centered "Placeholder" label) in a macOS window with custom title bar and traffic lights. Grid layout unit test passes.

Output: Full rendering pipeline with quad + text, single-panel grid layout, and platform integration.
</objective>

## Phase Goal

**As a** developer, **I want to** see and interact with a resizable grid of panels in a signed macOS application, **so that** I have a working workspace skeleton to build terminal, canvas, and document panels on top of.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-window-grid-and-build-pipeline/01-RESEARCH.md
@.planning/phases/01-window-grid-and-build-pipeline/01-PATTERNS.md
@.planning/phases/01-window-grid-and-build-pipeline/01-CONTEXT.md
@.planning/phases/01-window-grid-and-build-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 -- interfaces the executor needs to work with -->

From src/renderer/gpu_state.rs:
```rust
pub struct GpuState {
    pub surface: wgpu::Surface<'static>,
    pub device: wgpu::Device,
    pub queue: wgpu::Queue,
    pub config: wgpu::SurfaceConfiguration,
    pub window: Arc<Window>,
    pub format: wgpu::TextureFormat,
}

impl GpuState {
    pub async fn new(window: Arc<Window>) -> Self;
    pub fn resize(&mut self, width: u32, height: u32);
}
```

From src/renderer/mod.rs:
```rust
pub struct Renderer {
    gpu_state: GpuState,
}

impl Renderer {
    pub fn new(window: Arc<Window>) -> Self;
    pub fn resize(&mut self, width: u32, height: u32);
    pub fn render(&mut self, clear_color: [f32; 4]) -> Result<(), wgpu::SurfaceError>;
    pub fn device(&self) -> &wgpu::Device;
    pub fn queue(&self) -> &wgpu::Queue;
    pub fn format(&self) -> wgpu::TextureFormat;
}
```

From src/theme.rs:
```rust
pub struct Theme {
    pub background: [f32; 4],
    pub panel_background: [f32; 4],
    pub title_bar_text: [f32; 4],
    pub divider: [f32; 4],
    pub divider_hover: [f32; 4],
    pub panel_label_text: [f32; 4],
}
```

From src/shaders/quad.wgsl:
```wgsl
struct QuadInstance {
    @location(0) position: vec2<f32>,
    @location(1) size: vec2<f32>,
    @location(2) color: vec4<f32>,
    @location(3) corner_radius: f32,
};
struct Uniforms { viewport_size: vec2<f32> };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build quad renderer, grid layout, and panel model with unit tests</name>
  <files>
    src/renderer/quad_renderer.rs
    src/renderer/mod.rs
    src/grid/mod.rs
    src/grid/layout.rs
    src/grid/panel.rs
    src/platform/mod.rs
    src/platform/macos.rs
  </files>
  <read_first>
    .planning/phases/01-window-grid-and-build-pipeline/01-RESEARCH.md
    .planning/phases/01-window-grid-and-build-pipeline/01-PATTERNS.md
    src/renderer/mod.rs
    src/renderer/gpu_state.rs
    src/shaders/quad.wgsl
    src/theme.rs
  </read_first>
  <action>
Build the quad renderer, grid layout engine, panel model, and macOS platform layer. Follow RESEARCH.md patterns exactly.

**src/renderer/quad_renderer.rs** -- Per PATTERNS.md quad_renderer section + RESEARCH Pattern 4:
- `QuadInstance` struct: `#[repr(C)] #[derive(Copy, Clone, Debug, bytemuck::Pod, bytemuck::Zeroable)]` with position: [f32; 2], size: [f32; 2], color: [f32; 4], corner_radius: f32, plus padding: f32 for 16-byte alignment
- `Uniforms` struct: `#[repr(C)] #[derive(Copy, Clone, bytemuck::Pod, bytemuck::Zeroable)]` with viewport_size: [f32; 2]
- `QuadRenderer` struct with pipeline, uniform_buffer, uniform_bind_group, instance_buffer, instance_count
- `QuadRenderer::new(device, format)`: create shader module from include_str!("../shaders/quad.wgsl"), create uniform buffer, create bind group layout + bind group for uniforms, create render pipeline with vertex state (no vertex buffers, instances via @location attributes on the instance buffer), instance buffer layout matching QuadInstance fields, fragment state targeting the surface format
- Instance buffer layout: step_mode = Instance, attributes at locations 0-4 mapping to position, size, color, corner_radius
- `prepare(&mut self, device, queue, quads: &[QuadInstance], viewport_width, viewport_height)`: write uniforms to queue, recreate or write instance buffer with quad data, store instance_count
- `render<'pass>(&'pass self, pass: &mut RenderPass<'pass>)`: set pipeline, set bind group 0, set vertex buffer 0 (instance buffer), draw(0..6, 0..instance_count)

**Modify src/renderer/mod.rs**:
- Add `pub mod quad_renderer;`
- Add `quad_renderer: QuadRenderer` field to Renderer
- Initialize QuadRenderer in Renderer::new() using device and format from GpuState
- Update render() method to accept `quads: &[QuadInstance]` and viewport dimensions
- In render(): call quad_renderer.prepare() with quads, then call quad_renderer.render() in the render pass
- Cap maximum instance count to 1000 quads to prevent GPU buffer exhaustion (T-01-02 threat mitigation)

**src/grid/mod.rs**:
```rust
pub mod layout;
pub mod panel;

pub use layout::GridLayout;
pub use panel::{Panel, PanelId, PanelType};
```

**src/grid/layout.rs** -- Per PATTERNS.md grid/layout section:
- `GridLayout` struct with tree: TaffyTree<()>, root: NodeId, panels: Vec<(NodeId, PanelId)>, next_id: u64
- `new_single_panel()`: create TaffyTree, one leaf, root with Display::Grid, grid_template_columns: vec![fr(1.0)], grid_template_rows: vec![fr(1.0)], size 100% x 100%
- `compute(&mut self, width: f32, height: f32)`: compute_layout with Definite available space
- `get_panel_rect(&self, node: NodeId) -> (f32, f32, f32, f32)`: return (x, y, width, height) from taffy Layout
- `panel_nodes(&self) -> &[(NodeId, PanelId)]`: return reference to panels vec
- Include unit test: `test_single_panel_fills_window` -- compute at 1280x800, assert rect is (0, 0, 1280, 800)

**src/grid/panel.rs** -- Per PATTERNS.md panel section:
- `PanelId(pub u64)` with Debug, Clone, Copy, PartialEq, Eq, Hash
- `Panel` struct with id: PanelId, panel_type: PanelType, title: String
- `PanelType` enum with Placeholder variant
- `impl Panel { pub fn new_placeholder(id: PanelId) -> Self }`

**src/platform/mod.rs**:
```rust
#[cfg(target_os = "macos")]
pub mod macos;
```

**src/platform/macos.rs** -- Per PATTERNS.md macos section exactly:
- `pub fn setup_custom_title_bar(window: &winit::window::Window)` function
- Get RawWindowHandle::AppKit, cast to &NSWindow
- Set title visibility hidden
- Reposition traffic lights: offset_x = 12.0, offset_y = 16.0
- Iterate CloseButton, MiniaturizeButton, ZoomButton and setFrameOrigin with offsets
- Apply positioning in both `resumed()` and after window resize per Open Question 4 resolution

Critical anti-patterns to avoid:
- Do NOT use `with_decorations(false)` -- removes traffic lights (Pitfall 3)
- Do NOT pass zero dimensions to wgpu (Pitfall 2) -- guard all resize/configure calls
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/myco && cargo build 2>&amp;1 | tail -5 && cargo test --lib 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    QuadRenderer created with instanced drawing pipeline. GridLayout wraps taffy CSS Grid with single-panel layout. Panel model defined. macOS platform layer repositions traffic lights. `cargo build` succeeds and `cargo test --lib` passes the grid layout unit test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add glyphon text rendering and wire everything into the render loop</name>
  <files>
    src/renderer/text_renderer.rs
    src/renderer/mod.rs
    src/app.rs
  </files>
  <read_first>
    .planning/phases/01-window-grid-and-build-pipeline/01-PATTERNS.md
    src/renderer/mod.rs
    src/renderer/gpu_state.rs
    src/renderer/quad_renderer.rs
    src/grid/layout.rs
    src/grid/panel.rs
    src/app.rs
  </read_first>
  <action>
Add text rendering using glyphon and wire everything (grid, quads, text) into the app render loop. Follow PATTERNS.md text_renderer section exactly.

**src/renderer/text_renderer.rs** -- Per PATTERNS.md text_renderer section:
- `TextEngine` struct with: font_system: FontSystem, swash_cache: SwashCache, atlas: TextAtlas, text_renderer: TextRenderer, viewport: Viewport
- `TextEngine::new(device, queue, format)`: FontSystem::new() (loads system fonts), SwashCache::new(), TextAtlas::new(device, queue, format), TextRenderer::new(&mut atlas, device, MultisampleState::default(), None), Viewport::new(device, queue) -- Note: check glyphon 0.11.0 API. If `Viewport::new` needs a `&wgpu::Queue`, pass it. If the API uses a `Cache` object instead of `Viewport`, adapt accordingly.
- `TextEngine::prepare(&mut self, device, queue, labels: &[TextLabel], width: u32, height: u32)` where TextLabel is a struct: `pub struct TextLabel { pub text: String, pub x: f32, pub y: f32, pub width: f32, pub height: f32, pub font_size: f32, pub color: glyphon::Color }`
  - Update viewport with Resolution { width, height }
  - For each label: create a Buffer with Metrics::new(font_size, font_size * 1.3), set_size, set_text with Attrs::new().family(Family::SansSerif), shape_until_scroll
  - Build Vec<TextArea> from the buffers with correct bounds
  - Call self.text_renderer.prepare(device, queue, &mut font_system, &mut atlas, &viewport, text_areas, &mut swash_cache)
  - IMPORTANT: The glyphon Buffer objects must live long enough for the TextArea references. Store them in a Vec<Buffer> field on TextEngine that gets cleared and rebuilt each frame.
- `TextEngine::render<'pass>(&'pass self, pass: &mut RenderPass<'pass>)`: call self.text_renderer.render(&atlas, &viewport, pass)

**Modify src/renderer/mod.rs**:
- Add `pub mod text_renderer;`
- Add `text_engine: TextEngine` field to Renderer
- Initialize TextEngine in Renderer::new()
- Update render() method signature to also accept `labels: &[TextLabel]`
- In render(): after quad_renderer.render(), call text_engine.prepare() then text_engine.render() -- text renders ON TOP of quads in the same render pass

**Modify src/app.rs** -- Major wiring update:
- Add fields to App: `grid: Option<GridLayout>`, `panels: Vec<Panel>`
- In resumed(): init `GridLayout::new_single_panel()`, create one `Panel { id: PanelId(0), panel_type: PanelType::Placeholder, title: "Placeholder".into() }`, call `platform::macos::setup_custom_title_bar()`
- In Resized handler: also recompute grid layout
- In RedrawRequested handler, the render sequence is:
  1. Get window physical size
  2. Call `grid.compute(width as f32, height_minus_titlebar as f32)` -- reserve top 38px for custom title bar area
  3. For each panel, get rect from `grid.get_panel_rect(node)`, offset y by title bar height (38px)
  4. Build `Vec<QuadInstance>` for: title bar background quad (full width, 38px tall, theme.background color), panel background quads (theme.panel_background color), panel title bar quads (thin area at top of each panel for the cap label per D-01/D-02)
  5. Build text labels:
     - Title bar breadcrumb (per D-14): TextLabel at x=80 (after traffic lights), y=10, text="Myco > Untitled Project", font_size=13.0, color=theme.title_bar_text
     - For each panel: centered type label (per D-01/D-03): TextLabel at center of panel rect, text=panel.title (e.g., "Placeholder"), font_size=14.0, color=theme.panel_label_text
     - For each panel: title bar label (per D-01): TextLabel at panel rect top-left + small offset (x+8, y+4), text=panel.panel_type display name, font_size=12.0, color=theme.title_bar_text
  6. Pass both quads and labels to renderer.render()

The centered label positioning: for a panel at (px, py, pw, ph), place the label at approximately (px + pw/2 - text_width_estimate/2, py + ph/2 - font_size/2). Use pw as the TextLabel width and let glyphon handle horizontal centering via text alignment if available, or estimate based on character count * 0.5 * font_size.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/myco && cargo build 2>&amp;1 | tail -5 && cargo test --lib 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    Running the binary shows a macOS window with: (1) custom title bar displaying "Myco > Untitled Project" breadcrumb text, (2) a single themed dark panel filling the window body, (3) centered "Placeholder" type label in the panel, (4) panel title bar area with type label text. All text is GPU-rendered via glyphon. Traffic light buttons are visible and functional. `cargo build` exits 0 and `cargo test --lib` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS events -> App | Window events from macOS (resize, input) enter the application |
| wgpu -> GPU | Shader code and buffer data sent to Metal GPU driver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | src/renderer/quad_renderer.rs | mitigate | Cap maximum instance count to prevent GPU buffer exhaustion: limit to 1000 quads (far exceeds panel count) |
| T-01-04 | Elevation of Privilege | src/platform/macos.rs | accept | unsafe block for NSWindow cast from raw handle -- unavoidable for objc2 interop, pointer comes from winit (trusted source) |
</threat_model>

<verification>
- `cargo build` succeeds with zero errors
- `cargo test --lib` passes all unit tests (grid layout test)
- Running the binary (`cargo run`) opens a window showing a themed panel with text labels
- Window has custom transparent title bar with native traffic light buttons
- "Myco > Untitled Project" breadcrumb visible in title bar
- "Placeholder" label centered in panel body
- Resizing the window causes the panel to reflow (no crash, no black borders)
</verification>

<success_criteria>
- Cargo project compiles and runs on macOS
- QuadRenderer renders instanced colored rectangles via GPU
- TextEngine renders text labels via glyphon
- Single themed panel fills the window body with dark background
- "Myco > Untitled Project" breadcrumb visible in title bar area
- "Placeholder" label centered in panel body
- Panel type label in panel title bar area
- Window resize works without crash (zero-dimension guard active)
- Grid layout unit test passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-window-grid-and-build-pipeline/01-02-SUMMARY.md`
</output>
